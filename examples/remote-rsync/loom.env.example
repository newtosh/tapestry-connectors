# Copy to loom.env in your private scripts directory (do not commit).

LOOM_HOST=macmini.example.tailnet.ts.net
LOOM_SSH_USER=your-username

# Path to Source/ inside the git clone ON THE MAC MINI (Loom watches this folder)
LOOM_PATH='~/Developer/tapestry-connectors/Source'
